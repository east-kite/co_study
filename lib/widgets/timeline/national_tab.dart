import 'package:flutter/material.dart';

class NationalTab extends StatelessWidget {
  const NationalTab({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('全国ランキング'));
  }
}
