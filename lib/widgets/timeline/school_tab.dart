import 'package:flutter/material.dart';

class SchoolTab extends StatelessWidget {
  const SchoolTab({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('校内ランキング'));
  }
}
